[build]
  publish = "public"
  functions = "netlify/functions"

# `netlify dev` serves the static page + functions together on one port.
[dev]
  framework = "#static"
  publish = "public"
  port = 5599
